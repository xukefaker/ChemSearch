#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CMD="$ROOT/.venv/bin/paperscout"

if [[ ! -x "$CMD" ]]; then
  printf 'PaperScout is not installed yet. Run ./scripts/install.sh first.\n' >&2
  exit 1
fi

exec "$CMD" "$@"
